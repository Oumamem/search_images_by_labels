export class ImageRepository {
    analyze(keyword, labels) {
        //		const urls = await getUnsplashUrls(keyword);
        //		const urlsResponse = await getGoogleVisionlabels(labels, urls);
        //		return urlsResponse;
        return ["x"];
    }
}
//# sourceMappingURL=imageRepository.js.map